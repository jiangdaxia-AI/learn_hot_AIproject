# 03-06 · 智能路由层 — 大白话教材

> **面向读者**：产品经理、运营人员 | **难度**：⭐⭐ 进阶 | **阅读时间**：约 15 分钟

---

## 一句话说清楚

智能路由层是 OpenSquilla 的"智能调度员"。每次用户发消息，它先判断任务难度，然后决定用哪个 AI 模型——简单问题用便宜模型，复杂问题才用贵模型。这能大幅降低 AI 使用成本。

---

## 生活化比喻

> 智能路由层像**滴滴的智能派单系统**。你去楼下便利店（简单任务），系统派一辆共享单车（便宜模型）；你要去机场（复杂任务），系统派一辆专车（贵模型）。系统通过分析你的历史行程（历史对话）、当前时间地点（上下文特征）、目的地（任务复杂度），自动做出最优决策。

---

## 一、它怎么判断任务难度？

路由层在本地运行一个轻量级的"智能分类器"（基于决策树算法，叫 LightGBM），每次判断只需要几毫秒，不消耗网络请求。它从用户消息中提取 8 个维度的特征来判断任务难度：

### 8 个判断维度（就像快递分拣的 8 道扫描）

| 维度 | 看什么 | 大白话 |
|------|--------|--------|
| 1. 文本统计 | 字数、句子长短、问号数量 | 看消息有多长、多复杂 |
| 2. 代码特征 | 有没有代码、什么语言、代码行数 | 是不是在写代码？ |
| 3. 关键词 | 有没有"搜索"、"分析"、"实现"、"修复"等词 | 通过动词判断任务类型 |
| 4. 复杂度 | 大写字母比例、特殊符号密度、链接数量 | 内容是否"技术含量高" |
| 5. 格式特征 | 列表项数、标题层级、引用块数 | 消息结构是否复杂 |
| 6. 上下文 | 当前第几轮对话、上下文有多长、用了多少工具 | 聊天深度如何？ |
| 7. 历史轨迹 | 上一轮用的什么模型、难度变化趋势、切换次数 | 历史表现怎样？ |
| 8. 深层语义 | 用 AI 理解文字的真实含义 | 语义层面是否复杂？ |

---

## 二、4 级难度分类

| 难度级别 | 难度 | 决策 | 适合什么任务 | 用的模型 |
|---------|------|------|------------|---------|
| R0（简单） | 简单 | 最便宜模型 | 闲聊、简单问答、格式转换 | deepseek-v4-flash |
| R1（中等） | 中等 | 中档模型 | 代码编写、文档生成、数据分析 | deepseek-v4-pro |
| R2（困难） | 困难 | 高档模型 | 复杂算法、架构设计、多文件工程 | kimi-k2.7-code |
| R3（极难） | 极难 | 最强模型 | 需要最强推理能力的任务 | glm-5.2 |

---

## 三、路由决策的完整流程——从消息到模型选择

整个过程就像**快递分拣系统**：

```
用户消息 "帮我写一个排序算法"
  │
  ▼
步骤 1：收件
  收到消息，先看看是不是特殊件（有图片？手动指定了模型？）
  │
  ▼
步骤 2：8 通道扫描
  用 8 个维度扫描消息，提取约 390 个特征值
  （字数、代码行数、关键词、历史轨迹、语义深度...）
  │
  ▼
步骤 3：三台"分拣机"同时判断
  ├─ 主分拣机：看过最多样本，最有经验
  ├─ 辅助分拣机：专门看"是不是该降级"
  └─ 语义分拣机（神经网络）：从语义角度补充判断
  │
  三台机器的结果按权重融合
  │
  ▼
步骤 4：人工复核（8 条安全规则）
  ├─ 规则1：选概率最高的级别
  ├─ 规则2：不确定时宁可升级（多花点钱，别用弱模型处理复杂任务）
  ├─ 规则3：辅助模型判断是否应该降级
  ├─ 规则4：R0和R1差距太小时，提到R1（避免简单任务被低估）
  ├─ 规则5：如果R2+R3概率之和超过45%，强制提到R2（安全底线）
  ├─ 规则6：特殊标记强制提升（高风险内容→至少R2）
  ├─ 规则7：深层次对话（第4轮以上）→至少R1
  └─ 规则8：如果上一轮用了高级模型，本轮尽量不动（避免切换开销）
  │
  ▼
步骤 5：打印面单
  决定用什么模型、要不要开启"思考模式"、用什么提示词策略
  │
  ▼
最终输出：C0 → deepseek-v4-flash（发货！）
```

---

## 四、用户可以手动切换模型

用户可以通过命令手动指定模型，比如想用高级模型处理复杂任务：

| 操作 | 说明 |
|------|------|
| 临时切换 | 手动指定用哪个档位的模型，一段时间后自动恢复 |
| 恢复自动 | 让系统重新自动判断任务难度 |

---

## 五、模型自学习闭环——"越用越聪明"

智能路由层不是"训练好就固定不变"的静态模型。它有一套完整的**自学习闭环**（也叫数据飞轮），能在使用过程中持续改进。

### 飞轮全景图

```
 ┌──────────────────────────────────────────────────┐
 │                                                  │
 │   ① 每次路由决策 → 记录训练样本（不存原文，保护隐私）│
 │      （只存特征向量，约 0.78 KB）                  │
 │                                                  │
 │   ② 用户反馈（点赞👍 / 点踩👎）                    │
 │      点踩会被特别重视——"刚才那个判断错了！"          │
 │                                                  │
 │   ③ 定期重新训练（用户不活跃时，后台自动进行）       │
 │      至少间隔 72 小时，不会频繁训练                 │
 │                                                  │
 │   ④ 新模型无缝切换（用户无感知）                    │
 │      自动检查新旧兼容性，不兼容就回退                │
 │                                                  │
 │   ⑤ 在线监控 + 自动回滚                             │
 │      投诉率或点踩率上升 → 自动回退到旧版本          │
 │                                                  │
 └──────────────────────────────────────────────────┘
```

### 大白话总结

> 这个自学习闭环就像**自动驾驶汽车的持续改进系统**：每次行车都记录决策 → 乘客可以点赞/点踩 → 闲置时后台分析数据重新学习 → 新模型无缝切换 → 如果数据变差自动回退。整个过程用户完全无感。

---

## 六、对产品/运营同学的一句话总结

> 智能路由层让 AI 使用成本"能省则省"。简单聊天用便宜模型（几厘钱），复杂任务才用贵模型（几分钱），平均能节省 60% 以上的模型调用费用。而且它越用越聪明，会根据用户反馈自动优化判断。用户也可以手动切换模型档位。

---

## 七、路由被绕过的场景

### 场景 1：编码模式自动绕过

当编码模式（Coding Mode）开启时，系统通过 `router_control` 的 `set_hold` 机制锁定模型为 `tier:c1`（deepseek-v4-pro）。此后所有请求都跳过路由器，直接使用固定模型。

**实际日志表现**：

```
# 正常模式：路由器在决策
2026-07-01 11:39:55 squilla_router.routed
  probabilities: R0=59.5%  R1=33.4%  → 选了便宜模型

# 编码模式：路由器被绕过
2026-07-24 14:15:09 resolve_model.applied model='deepseek-v4-pro'
  （没有 squilla_router.routed）
```

### 场景 2：用户手动锁定

用户可以通过 `router_control` 命令手动锁定模型档位，路由器同样被绕过。日志中 `set_hold` 出现了 2 次。

### 实际运行数据

| 时间段 | 路由决策 | 使用的模型 | 说明 |
|--------|:---:|------|------|
| 7月1-2日 | 177 次 | 豆包/R1，正常切换 | 路由正常工作 |
| 7月15-24日 | **0 次** | 全部 deepseek-v4-pro | 路由被绕过 |

### 影响

路由被绕过时，所有任务（包括简单问题）都使用同一个模型，失去了"省钱"的优化效果。但因为模型固定，缓存命中率反而更高（83%）。这是**成本 vs 缓存**之间的 trade-off。

### 源码对应

- `router_control`：`set_hold` / `clear_hold` 操作
- `engine/steps/resolve_model.py`：`resolve_model.applied` 日志事件
- 当 hold 生效时，`resolve_model` 直接返回固定模型，不调用路由器

---

## 八、路由模型内部架构（18 个文件）

### 推理引擎（`models/v4.2_phase3_inference/runtime_src/src/router/`）

| 文件 | 职责 |
|------|------|
| `bge_onnx.py` | BGE ONNX 模型：运行时语义特征提取（第 8 维度"深层语义"） |
| `features.py` | 8 维度特征提取：文本统计、代码特征、关键词、复杂度、格式、上下文、历史轨迹、语义 |
| `flags.py` | 特征开关：控制哪些维度参与路由决策 |
| `model_io.py` | 模型 I/O：LightGBM 模型的加载和保存 |
| `anti_downgrade.py` | 反降级规则：8 条安全规则的实现 |
| `calibration.py` | 校准：概率校准和置信度调整 |
| `inference/__init__.py` | 推理入口 |
| `inference/core.py` | 核心推理：三台"分拣机"的编排 |
| `inference/lgbm.py` | LightGBM 推理：主分拣机 |
| `inference/pre.py` | 预处理：特征标准化 |
| `inference/post.py` | 后处理：概率→分类映射 |
| `inference/voting.py` | 投票融合：三台分拣机结果按权重融合 |
| `inference/confidence.py` | 置信度门控：置信度不足时触发升级 |

### 测试文件

| 文件 | 职责 |
|------|------|
| `test_router.py` | 路由单元测试 |
| `test_router_integration.py` | 路由集成测试 |

---

## 九、自学习系统（8 个文件）

| 文件 | 职责 |
|------|------|
| `collector.py` | 数据收集：从路由决策和用户反馈中收集训练样本 |
| `data_flywheel.py` | 数据飞轮：驱动自学习循环（收集→训练→部署→收集） |
| `feedback.py` | 反馈处理：用户手动切换模型 → 记录为训练信号 |
| `schema.py` | 数据模式：训练样本的字段定义 |
| `state.py` | 状态管理：自学习循环的状态机 |
| `store.py` | 存储：训练样本的持久化 |
| `train.py` | 训练触发：何时启动训练、训练参数 |
| `train_worker.py` | 训练工作器：异步训练执行 |

---

## 十、路由控制器（`controller.py`）

`router_control` 的 `set_hold` / `clear_hold` 实现。当 hold 生效时，`resolve_model` 直接返回固定模型，不调用路由器。详见 §七。

---

## 十一、主路由入口（`v4_phase3.py`）

`SquillaRouter` 主类：整合特征提取、模型推理、安全规则、置信度门控的完整流程入口。

### 路由模块覆盖总结

| 类别 | 文件数 | 覆盖状态 |
|------|:---:|:---:|
| 推理引擎 | 18 | ✅ 全部覆盖 |
| 自学习系统 | 8 | ✅ 全部覆盖 |
| 控制器 | 1 | ✅ 全部覆盖 |
| 主入口 | 1 | ✅ 全部覆盖 |
| **总计** | **34** | **✅ 100%** |
